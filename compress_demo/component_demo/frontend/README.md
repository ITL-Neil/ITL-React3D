# Frontend Demo Placeholder

This folder is reserved for a future frontend demo that imports:

```bash
component/frontend/DragUpload.tsx
```

The planned demo page will:

- render the reusable drag-and-drop upload component
- restrict uploads to `.glb` files for compression testing
- send the selected file to the demo backend
- download the compressed GLB file returned by the backend

Planned startup command:

```bash
cd component_demo/frontend
npm install
npm run dev
```

Demo source code is not implemented in this step.
